<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cola.machine.dao.SysRolePermissionMapper">
  <resultMap id="BaseResultMap" type="cola.machine.bean.SysRolePermission">
      <id column="id" jdbcType="BIGINT" property="id" />
      <result column="rid" jdbcType="BIGINT" property="rid" />
      <result column="pid" jdbcType="BIGINT" property="pid" />
  </resultMap>
  <sql id="Base_Column_List">
    sys_role_permission.`id` ,sys_role_permission.`rid` ,sys_role_permission.`pid` 
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from sys_role_permission
    where sys_role_permission.id =   #{id,jdbcType=BIGINT}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from sys_role_permission
    where id = #{id,jdbcType=BIGINT}
  </delete>
   <delete id="deleteExtra" parameterType="map">
          delete from sys_role_permission
          where rid in
          <foreach collection="rids" item="item" index="index"
                   open="(" separator="," close=")"> #{item}</foreach>
         <if test="pids != null and id != '' " >
          and pid not in
          <foreach collection="pids" item="item" index="index"
                   open="(" separator="," close=")"> #{item}</foreach>
       </if>
      </delete>
 <insert id="insert" useGeneratedKeys="true" keyProperty="id"   parameterType="cola.machine.bean.SysRolePermission" >

    insert into sys_role_permission (  <include refid="Base_Column_List" />)
    values (
#{id,jdbcType=BIGINT}
,#{rid,jdbcType=BIGINT}
,#{pid,jdbcType=BIGINT}
    )
  </insert>
   <insert id="insertSelective" parameterType="cola.machine.bean.SysRolePermission" >
    insert into sys_role_permission
    <trim prefix="(" suffix=")" suffixOverrides="," >
        <if test="id != null" >  
           `id`, 
        </if>  
        <if test="rid != null" >  
           `rid`, 
        </if>  
        <if test="pid != null" >  
           `pid`, 
        </if>  
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
        <if test="id != null" >  
          #{id,jdbcType=BIGINT},
        </if>  
        <if test="rid != null" >  
          #{rid,jdbcType=BIGINT},
        </if>  
        <if test="pid != null" >  
          #{pid,jdbcType=BIGINT},
        </if>  
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="cola.machine.bean.SysRolePermission" >
    update sys_role_permission
    <set >
        <if test="id != null" >  
             `id`=#{id,jdbcType=BIGINT},
        </if>  
        <if test="rid != null" >  
             `rid`=#{rid,jdbcType=BIGINT},
        </if>  
        <if test="pid != null" >  
             `pid`=#{pid,jdbcType=BIGINT},
        </if>  
    </set>
    where id = #{id,jdbcType=BIGINT}
  </update>
  <update id="updateByPrimaryKey" parameterType="cola.machine.bean.SysRolePermission" >
    update sys_role_permission
    set 
              `rid`=#{rid,jdbcType=BIGINT},  
              `pid`=#{pid,jdbcType=BIGINT}  
where id = #{id,jdbcType=BIGINT}
  </update>
  <select id="listByParams" parameterType="map" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from sys_role_permission where 1=1
        <if test="id != null and id != '' ">  
           and `id` = #{id}
        </if>  
        <if test="rid != null and rid != '' ">  
           and `rid` = #{rid}
        </if>  
        <if test="pid != null and pid != '' ">  
           and `pid` = #{pid}
        </if>  
  </select>
   <select id="listByParams4Page" parameterType="map" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from sys_role_permission

    where 1=1
        <if test="id != null and id != '' ">  
           and sys_role_permission.`id` = #{id}
        </if>  
        <if test="rid != null and rid != '' ">  
           and sys_role_permission.`rid` = #{rid}
        </if>  
        <if test="pid != null and pid != '' ">  
           and sys_role_permission.`pid` = #{pid}
        </if>  
  </select>
   <select id="countByParams" parameterType="map" resultType="java.lang.Integer">
    select 
    count(1) 
    from sys_role_permission where 1=1
        <if test="id != null and id != '' ">  
           and sys_role_permission.`id` = #{id}
        </if>  
        <if test="rid != null and rid != '' ">  
           and sys_role_permission.`rid` = #{rid}
        </if>  
        <if test="pid != null and pid != '' ">  
           and sys_role_permission.`pid` = #{pid}
        </if>  
  </select>
 <select id="countByOrParams" parameterType="map" resultType="java.lang.Integer">
    select
    count(1)
    from sys_role_permission where 1!=1
        <if test="id != null and id != '' ">
           or `id` = #{id}
        </if>
        <if test="rid != null and rid != '' ">
           or `rid` = #{rid}
        </if>
        <if test="pid != null and pid != '' ">
           or `pid` = #{pid}
        </if>
  </select>
</mapper>
